[TEMPLATE]
# Backend API base URL (required in production)
# For local development, the Vite proxy handles /api/v1 -> localhost:5000
VITE_API_BASE_URL=https://neuro-server.vercel.app/api/v1

# Google Sign-In client ID (optional, enables Google OAuth)
VITE_GOOGLE_CLIENT_ID=
